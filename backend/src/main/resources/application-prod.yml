spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/futsal_team_manager
    username: futsal_user_prod
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    locations: classpath:db/migration

app:
  cors:
    allowed-origins: http://192.168.1.180,https://burns-nowhere-into-walt.trycloudflare.com
    allowed-methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
